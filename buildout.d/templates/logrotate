${buildout:directory}/var/log/instance*-Z2.log {
  rotate 10
  weekly
  compress
  delaycompress
  postrotate
    /sbin/kill -USR2 `cat ${buildout:directory}/var/instance1.pid`
    /sbin/kill -USR2 `cat ${buildout:directory}/var/instance2.pid`
  endscript
  su ${users:zope-process}
}
